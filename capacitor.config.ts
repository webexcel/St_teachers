import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.schooltree.schooltree',
  appName: 'Schooltree-teachers',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
